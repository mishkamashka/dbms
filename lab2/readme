как оно работает
. set_env.sh (не ./set_env.sh, ибо ./ исполняется в sub-shell'e,  переменные нужны в основном)
./create_db.sh

если что-то пойдет не так, то чтобы запустить заново надо сначала сделать clean_files.sh, а то бд не создается, если файлы уже существуют

чтоб все вырубить надо сделать shutdowm immediate; в sqlplus

это все надо запускать с oracle@db114, туда подключаться с гелиоса по ssh